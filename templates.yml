- name: Template Demo
  hosts: localhost
  connection: local
  vars:
    fruit_name: apple
  tasks:
    - name: Create Template
      ansible.builtin.template:
        src: test.txt
        dest: /tmp/test.txt